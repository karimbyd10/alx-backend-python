# commands.txt

# Install the NGINX Ingress controller (Minikube)
minikube addons enable ingress

# Apply the Ingress resource
yaml_file="ingress.yaml"
kubectl apply -f $yaml_file

# Verify the Ingress\ nkubectl get ingress

